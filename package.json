{
  "name": "brimble-sample",
  "private": true,
  "version": "0.1.0",
  "description": "Deterministic Brimble infra probe for archive and Git deployment demos",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "node --check server.js",
    "postinstall": "node -e \"require('node:fs').mkdirSync('node_modules', { recursive: true })\"",
    "start": "node server.js"
  }
}
